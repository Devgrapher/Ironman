<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin" tools:context=".Main"
    android:clickable="false"
    android:background="#D1E0FF"
    android:id="@+id/mainLayout">

    <Button
        android:id="@+id/buttonConnect"

        android:text="@string/ButtonConnect"
        android:textColor="#FFFFFF"
        android:textSize="20sp"

        android:layout_width="wrap_content"
        android:layout_height="28dp"
        android:background="@drawable/button"
        android:shadowColor="#A8A8A8"
        android:shadowDx="0"
        android:shadowDy="0"
        android:shadowRadius="5"
        android:minWidth="200dp"
        android:onClick="onPair"
        android:layout_alignParentBottom="true"
        android:layout_alignStart="@+id/textViewSpeachResult" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/txtview_disconnected"
        android:textColor="#5D8091"
        android:id="@+id/textViewSpeachResult"
        android:minWidth="200dp"
        android:textSize="@dimen/font_size"
        android:gravity="center"
        android:layout_below="@+id/progressBarSpeech"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="35dp" />

    <ProgressBar
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/progressBarSpeech"
        android:indeterminate="false"
        android:progressDrawable="@drawable/progressbar"
        android:layout_centerVertical="true"
        android:layout_centerHorizontal="true"
        android:minWidth="200dp" />

</RelativeLayout>

